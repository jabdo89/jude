const initState = {
  students: [
    {
      studentError: null
    }
  ]
};
const studentReducer = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default studentReducer;
