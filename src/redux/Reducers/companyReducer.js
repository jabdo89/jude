const initState = {
  companys: [
    {
      companyError: null
    }
  ]
};

const companyReducer = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default companyReducer;
