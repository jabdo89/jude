const initState = {
  companys: [
    {
      jobOfferError: null
    }
  ]
};

const companyReducer = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default companyReducer;
